# =============================================================================
# Development (copy to .env and fill in)
# =============================================================================

# Database - local Postgres (e.g. docker-compose)
DATABASE_URL="postgresql://shopify:shopify_dev_password@localhost:5432/urgency_timer_dev"

# Shopify App Credentials (from Shopify Partner Dashboard)
SHOPIFY_API_KEY=your_api_key_here
SHOPIFY_API_SECRET=your_api_secret_here

# App URL (use ngrok or Cloudflare tunnel during development)
SHOPIFY_APP_URL=https://your-dev-url.ngrok-free.app

# Scopes (comma-separated)
SCOPES=write_products,read_products

# Optional: custom shop domain allowlist
# SHOP_CUSTOM_DOMAIN=myshop.com

# Node Environment (development for local; production on Render)
NODE_ENV=development

# =============================================================================
# Production (Render / Neon / other hosts)
# =============================================================================
# Set these in your host's dashboard (e.g. Render Environment). Do not commit.
#
# DATABASE_URL
#   - Render Postgres: use the "Internal Database URL" from Render dashboard.
#   - Neon: use the pooled connection string (recommended for serverless).
#     Example: postgresql://user:pass@ep-xxx-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require
#
# SHOPIFY_APP_URL
#   - Your production app URL, e.g. https://your-app-name.onrender.com
#
# NODE_ENV=production
#   - Required in production so the app validates env and uses production behavior
#     (e.g. real billing, strict proxy HMAC validation).
